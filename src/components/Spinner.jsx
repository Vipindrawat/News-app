import React from 'react'
import spinner from './Spinner.gif'

const Spinner=() => {
  
    return (
        <img className="m-auto py-5" src={spinner} alt="spinner image" />
    )
  }

  export default Spinner;
